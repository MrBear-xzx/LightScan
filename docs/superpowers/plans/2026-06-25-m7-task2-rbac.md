# M7 任务2（RBAC 角色权限模型）实施计划
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (- [ ]) syntax for tracking.

**Goal:** 实现基于角色的访问控制（RBAC），支持权限检查和角色级别校验。  
**Architecture:** 定义权限映射 + 角色层级，提供 FastAPI Depends 工厂函数。  
**Tech Stack:** FastAPI、PyJWT、pytest

## 任务拆解

1. 新增 app/auth/rbac.py：权限定义、require_permission、require_role。
2. 新增 auth_rbac 路由：测试用端点 + permissions 查询。
3. 注册到 main.py。
4. 新增测试：admin 通过、analyst 拒绝 admin、viewer 拒绝写入、analyst 写入通过、permissions 端点。
5. 更新 README。

## 验收标准

- [ ] equire_role('admin') 拦截非 admin 用户。
- [ ] equire_permission('vuln:write') 仅允许 analyst+。
- [ ] GET /api/v1/auth/permissions 返回用户拥有的权限列表。
- [ ] 定向测试通过。
