# M7 任务4（团队/项目空间）实施计划
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (- [ ]) syntax for tracking.

**Goal:** 提供项目空间管理，支持项目 CRUD 和团队成员管理。  
**Architecture:** 新增 projects 表和 project_members 表，提供 RESTful CRUD。  
**Tech Stack:** FastAPI、Pydantic、SQLAlchemy(SQLite auto-create)、pytest

## 任务拆解

1. 新增 Model：Project、ProjectMember。
2. 新增 schema：项目 CRUD + 成员管理。
3. 新增 project_service：项目/成员管理。
4. 新增路由：项目 CRUD + 成员管理。
5. 注册到 main.py。
6. 新增测试：CRUD 全流程。
7. 更新 README。

## 验收标准

- [ ] GET/POST/DELETE /api/v1/projects 项目 CRUD。
- [ ] GET/POST /api/v1/projects/{id}/members 成员管理。
- [ ] 定向测试通过。
